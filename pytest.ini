[pytest]
pythonpath = .
testpaths = tests
addopts = -ra
markers =
    hardware: requires a real machine or phone setup and is not expected to run in CI
    qt_runtime: exercises Qt/controller lifecycle behavior and may skip if host Qt runtime libraries are unavailable
